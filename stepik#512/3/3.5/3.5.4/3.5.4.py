# import json
# #написать ввод json формата
# si=[{"name": "A", "parents": []}, {"name": "B", "parents": ["A", "C"]}, {"name": "C", "parents": ["A"]}]
# datadumps=json.dumps(si, indent=4)
# dataloads=json.loads(datadumps)
# print(dataloads)
class B: pass
print(issubclass(B, B))
